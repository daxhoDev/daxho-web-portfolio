# URL canónica del sitio. Nunca hardcodear en el código (ADR-0004, ADR-0016).
# En local: http://localhost:4321 · En Vercel: la URL del despliegue.
SITE_URL="http://localhost:4321"

# --- Formulario de contacto (fase 7, todavía sin usar) ---
# ADR-0020: la cuenta de Resend DEBE registrarse con CONTACT_TO_EMAIL,
# porque el dominio de pruebas solo envía a la dirección de registro.
RESEND_API_KEY=""
CONTACT_TO_EMAIL="developer.daxho@gmail.com"
CONTACT_FROM_EMAIL="onboarding@resend.dev"
